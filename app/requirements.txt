fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0
pyyaml>=6.0,<7.0
pillow>=10.4,<12.0
httpx>=0.27,<1.0
markdown>=3.5,<4.0
bcrypt>=4.1,<5.0
itsdangerous>=2.1,<3.0

# ── Database stack (SQLAlchemy + Alembic + Postgres driver) ─────────────────
# Schema is managed with Alembic under app/migrations/. Runtime tables are
# queried via app/models and app/services. See docs/workspace-db-migration-plan.md
# for workspace ↔ DB migration notes.
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.1,<4.0
alembic>=1.13,<2.0
cryptography>=42,<46
structlog>=24,<26

# ── Test harness ──────────────────────────────────────────────────────────
# Kept pinned alongside runtime deps so the same image can run pytest in CI
# and ad-hoc inside the container. They are installed in production builds
# too, but unused unless `pytest` is invoked explicitly.
pytest>=8,<9
pytest-asyncio>=0.23,<1.0
